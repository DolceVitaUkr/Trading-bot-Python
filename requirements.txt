# --- Core ---
python-dotenv==1.0.1
orjson==3.10.7           # Fast JSON; on Windows requires Rust. If install fails, see note below.
ujson==5.10.0            # Optional fallback; safe to keep installed

# --- Web/API server ---
fastapi==0.115.2
uvicorn[standard]==0.30.6
httpx==0.27.2
websockets==12.0

# --- Exchanges / Brokers ---
pybit==5.6.2             # Bybit V5 official Python client
ib-insync==0.9.86        # Interactive Brokers TWS/CP API

# --- Telegram (async) ---
python-telegram-bot==21.7

# --- Data / Math / Indicators ---
pandas==2.2.2
numpy==1.26.4            # Pairs well with pandas 2.2 on most platforms
scipy==1.13.1
pandas-ta==0.3.14b0      # TA indicators in pandas (easier than TA-Lib binaries)

# --- ML / Optimization / Utilities ---
scikit-learn==1.5.2
deap==1.4.1              # Evolutionary algorithms for your EA_* params
statsmodels==0.14.2
loguru==0.7.2            # Better logging UX
rich==13.8.1             # Nice console formatting for logs / debug

# --- Optional / Nice-to-haves (uncomment if used) ---
# schedule==1.2.2         # Simple scheduler
# apscheduler==3.10.4     # Advanced scheduler
# plotly==5.24.1          # Rich plotting
# matplotlib==3.9.2       # Classic plotting
# joblib==1.4.2           # Model caching
# pydantic==2.9.2         # If you start validating config models

# --- Dev / Lint (optional) ---
# black==24.8.0
# isort==5.13.2
# flake8==7.1.1
